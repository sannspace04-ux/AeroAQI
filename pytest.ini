[pytest]
# Project root — pytest discovers tests from here
testpaths = tests

# Show short test IDs and a compact summary
addopts =
    -v
    --tb=short
    --no-header

# Markers (add more as needed)
markers =
    unit: pure unit tests — no network, no disk I/O
    integration: tests that hit real APIs or write real files
